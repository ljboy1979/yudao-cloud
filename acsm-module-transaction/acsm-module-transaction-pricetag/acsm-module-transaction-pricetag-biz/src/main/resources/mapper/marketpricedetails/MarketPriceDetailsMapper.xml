<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.acsm.module.transaction.pricetag.dal.mysql.marketpricedetails.MarketPriceDetailsMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <select id="findPriceByPriceId" resultType="cn.acsm.module.transaction.pricetag.dal.dataobject.marketprice.MarketPriceDO">
        select
        ifnull(MAX(price),0) as maxPrice ,
        ifnull(MIN(price),0) as minPrice,
        ifnull(SUM(price)/COUNT(id),0) as middlePrice
        from
        pricetag_market_price_details
        where market_price_id=#{id} and deleted=0
    </select>

</mapper>
