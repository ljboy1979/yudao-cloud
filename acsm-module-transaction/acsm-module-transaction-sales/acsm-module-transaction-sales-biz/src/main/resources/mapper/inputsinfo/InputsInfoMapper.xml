<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.acsm.module.transaction.sales.dal.mysql.inputsinfo.InputsInfoMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <select id="findSales" resultType="cn.acsm.module.transaction.sales.dal.dataobject.shelves.ShelvesSalesRespDo">
        select
        id ,
        inputs_name as name
        FROM sales_inputs_info
        where deleted = 0
        <if test=" salesName != null and salesName != '' ">
            AND inputs_name like CONCAT('%',#{salesName},'%')
        </if>
    </select>

    <select id="findSpecifications" resultType="cn.acsm.module.transaction.sales.dal.dataobject.shelves.ShelvesSalesRespDo">
        select
        siis.id ,
        siis.specifications_name AS name,
        siis.packaging_type AS packagingType,
        siis.packaging_type_name AS packagingTypeName,
        siis.number,
        siis.unit,
        siis.unit_name AS unitName,
        siis.packaging,
        siis.packaging_name AS packagingName,
        siis.measurement_unit AS measurementUnit,
        siis.measurement_unit_name AS measurementUnitName,
        siis.purchase_price AS purchasePrice,
        siis.selling_price AS sellingPrice,
        CONCAT(siis.number,siis.unit_name,"/",siis.packaging_name) AS specifications,
        sii.inputs_name AS salesName,
        sic.category_name AS classifyName
        FROM sales_inputs_info_specification siis
        LEFT FROM sales_inputs_info sii on siis.inputs_info_id = sii.id
        LEFT FROM sales_input_classify sic on sic.id = sii.inputs_kind
        where siis.deleted = 0 and sii.deleted = 0
        <if test=" salesId !=null and salesId!='' ">
          AND siis.inputs_info_id=#{salesId}
        </if>
        <if test=" salesName != null and salesName != '' ">
            AND sii.inputs_name like CONCAT('%',#{salesName},'%')
        </if>
    </select>

</mapper>
