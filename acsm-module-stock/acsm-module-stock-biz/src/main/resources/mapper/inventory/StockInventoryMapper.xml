<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.acsm.module.stock.dal.mysql.inventory.StockInventoryMapper">

    <select id="getList"
            resultType="cn.acsm.module.stock.api.inventory.vo.StockInventoryApiReqVO">
        select
            goods_id,
            goods_name,
            goods_type,
            packaging_type,
            packing_specification,
            inventory_quantity,
            measure_unit
        from
            stock_inventory
        where
            type = #{qo.type}
        AND deleted = 0
        AND goods_id = #{qo.goodsId}
        AND packing_specification = #{qo.packingSpecification}
    </select>

</mapper>
